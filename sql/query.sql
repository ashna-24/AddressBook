use query;

create table register(FullName varchar(30),
					  EmailID varchar(30),
					  UserName varchar(30),
					  Password varchar(30),
					  ConfirmPassword varchar(30));

select * from register